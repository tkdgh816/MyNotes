<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="MyNotes.Core.Views.TagsEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="using:MyNotes.Core.Views"
             xmlns:models="using:MyNotes.Core.Models"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             Width="350"
             Height="560">

  <Grid RowSpacing="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0"
          Padding="12,8,12,0">
      <TextBlock Text="Tags"
                 Style="{StaticResource SubtitleTextBlockStyle}" />
      <Button x:Name="VIEW_EditButton"
              Style="{StaticResource AppFontIconNormalButtonStyle}"
              HorizontalAlignment="Right"
              Content="&#xE70F;">
        <Button.Flyout>
          <MenuFlyout Placement="LeftEdgeAlignedTop">
            <MenuFlyoutItem Icon="Add"
                            Text="Add"
                            Click="VIEW_AddButtonMenuFlyoutItem_Click" />
            <MenuFlyoutItem Icon="Delete"
                            Text="Delete"
                            Click="VIEW_DeleteButtonMenuFlyoutItem_Click" />
          </MenuFlyout>
        </Button.Flyout>
      </Button>
    </Grid>

    <Grid Grid.Row="1">
      <AutoSuggestBox x:Name="VIEW_SearchAutoSuggestBox"
                      Margin="12,0"
                      PlaceholderText="Search tags"
                      QueryIcon="Find"
                      QuerySubmitted="VIEW_SearchAutoSuggestBox_QuerySubmitted" />
      <Grid x:Name="VIEW_AddTagGrid"
            RowSpacing="4"
            ColumnSpacing="4"
            Padding="12,4,12,12"
            Background="{StaticResource SubtleFillColorSecondaryBrush}"
            Visibility="Collapsed">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="New tags"
                   Grid.Row="0"
                   Grid.Column="0"
                   VerticalAlignment="Center" />
        <Button x:Name="VIEW_CloseAddModeButton"
                Grid.Row="0"
                Grid.Column="1"
                Style="{StaticResource AppFontIconNormalButtonStyle}"
                Content="&#xE711;"
                HorizontalAlignment="Right"
                Click="VIEW_CloseAddModeButton_Click" />
        <TextBox x:Name="VIEW_TagInputTextBox"
                 Grid.Row="1"
                 Grid.Column="0"
                 PlaceholderText="Enter a tag" />
        <Button x:Name="VIEW_AddTagButton"
                Grid.Row="1"
                Grid.Column="1"
                Content="Add"
                Style="{StaticResource AccentButtonStyle}"
                Click="VIEW_AddTagButton_Click" />
      </Grid>
    </Grid>

    <Grid Grid.Row="2">
      <SemanticZoom x:Name="VIEW_TagsSemanticZoom">
        <SemanticZoom.ZoomedOutView>
          <GridView x:Name="VIEW_TagsGroupGridView"
                    ItemsSource="{x:Bind TagsCollectionViewSource.View.CollectionGroups, Mode=OneWay}"
                    Margin="10,8,0,0">
            <GridView.ItemsPanel>
              <ItemsPanelTemplate>
                <ItemsWrapGrid Orientation="Horizontal"
                               MaximumRowsOrColumns="5"
                               HorizontalAlignment="Center" />
              </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerStyle>
              <Style TargetType="GridViewItem"
                     BasedOn="{StaticResource DefaultGridViewItemStyle}">
                <Setter Property="Margin"
                        Value="8" />
              </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Group.Key}"
                           FontSize="20" />
              </DataTemplate>
            </GridView.ItemTemplate>
          </GridView>
        </SemanticZoom.ZoomedOutView>

        <SemanticZoom.ZoomedInView>
          <ListView x:Name="VIEW_TagsListView"
                    ItemsSource="{x:Bind TagsCollectionViewSource.View, Mode=OneWay}"
                    SelectionMode="Multiple"
                    IsMultiSelectCheckBoxEnabled="False"
                    SelectionChanged="VIEW_TagsListView_SelectionChanged">
            <ListView.ItemsPanel>
              <ItemsPanelTemplate>
                <ItemsWrapGrid Orientation="Horizontal"
                               MaximumRowsOrColumns="2"
                               ItemWidth="170"
                               Margin="0,0,8,0" />
              </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
              <DataTemplate x:DataType="x:String">
                <Grid DataContext="{x:Bind}"
                      ColumnSpacing="4"
                      CornerRadius="{StaticResource ControlCornerRadius}">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0"
                             Text="{x:Bind}" />
                  <InfoBadge Grid.Column="1"
                             Value="5" />
                  <Grid.ContextFlyout>
                    <MenuFlyout Placement="Bottom">
                      <MenuFlyoutItem Text="Delete"
                                      Icon="Delete" />
                    </MenuFlyout>
                  </Grid.ContextFlyout>
                </Grid>
              </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.GroupStyle>
              <GroupStyle HidesIfEmpty="True">
                <GroupStyle.HeaderTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Key}" />
                  </DataTemplate>
                </GroupStyle.HeaderTemplate>
              </GroupStyle>
            </ListView.GroupStyle>
          </ListView>
        </SemanticZoom.ZoomedInView>
      </SemanticZoom>
    </Grid>

    <Grid x:Name="VIEW_CommandBarGrid"
          Grid.Row="3"
          Background="{StaticResource AcrylicInAppFillColorBaseBrush}"
          Padding="8"
          ColumnSpacing="6"
          MaxHeight="100">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <ScrollView x:Name="VIEW_CommandBarTagsScrollViewer"
                  Grid.Column="0"
                  ContentOrientation="Vertical"
                  HorizontalScrollBarVisibility="Hidden"
                  HorizontalScrollMode="Disabled"
                  Padding="0,0,8,0"
                  VerticalAlignment="Bottom">
        <ItemsRepeater ItemsSource="{x:Bind VIEW_TagsListView.SelectedItems}">
          <ItemsRepeater.Layout>
            <controls:WrapLayout HorizontalSpacing="4"
                                 VerticalSpacing="4" />
          </ItemsRepeater.Layout>

          <ItemsRepeater.ItemTemplate>
            <DataTemplate x:DataType="x:String">
              <views:AppTagButton x:Name="VIEW_SelectedTagButton"
                                  Content="{x:Bind}"
                                  Click="VIEW_SelectedTagButton_Click"
                                  DeleteButtonClick="VIEW_SelectedTagButton_DeleteButtonClick" />
            </DataTemplate>
          </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
      </ScrollView>

      <StackPanel Grid.Column="1"
                  VerticalAlignment="Bottom"
                  Spacing="4">
        <StackPanel x:Name="VIEW_CommandBarOptionsStackPanel" 
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Visibility="Collapsed">
          <Button x:Name="VIEW_CommandBarDeselectAllButton"
                  Style="{StaticResource AppFontIconNormalButtonStyle}"
                  Content="&#xE8E6;" 
                  Click="VIEW_CommandBarDeselectAllButton_Click"/>
          <Button Style="{StaticResource AppIconNormalButtonStyle}"
                  Padding="6">
            <ImageIcon Source="/Assets/icons/fluent/FluentShapeUnion24Regular-002.svg" />
            <Button.Flyout>
              <Flyout Placement="TopEdgeAlignedRight">
                <RadioButtons MaxColumns="2">
                  <RadioButton Content="And" />
                  <RadioButton Content="Or" />
                </RadioButtons>
              </Flyout>
            </Button.Flyout>
          </Button>
        </StackPanel>
        <Button x:Name="VIEW_CommandBarExploreButton"
                Content="Explore"
                Style="{StaticResource AccentButtonStyle}" 
                Click="VIEW_CommandBarExploreButton_Click"/>
      </StackPanel>
    </Grid>

    <TeachingTip x:Name="VIEW_DeleteTagsTeachingTip"
                 Target="{x:Bind VIEW_CommandBarGrid}"
                 Title="Delete selected tags"
                 PreferredPlacement="Top"
                 ActionButtonContent="Delete"
                 ActionButtonStyle="{StaticResource AccentButtonStyle}"
                 ActionButtonClick="VIEW_DeleteTagsTeachingTip_ActionButtonClick"
                 CloseButtonContent="Close"
                 IsLightDismissEnabled="True" />
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup x:Name="EditorModeStates">
        <VisualState x:Name="EditorModeNormal" />

        <VisualState x:Name="EditorModeAdd">
          <VisualState.Setters>
            <Setter Target="VIEW_SearchAutoSuggestBox.Visibility"
                    Value="Collapsed" />
            <Setter Target="VIEW_AddTagGrid.Visibility"
                    Value="Visible" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>

      <VisualStateGroup x:Name="SelectionStates">
        <VisualState x:Name="Unselected" />
        <VisualState x:Name="Selected">
          <VisualState.Setters>
            <Setter Target="VIEW_CommandBarOptionsStackPanel.Visibility"
                    Value="Visible" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
  </Grid>
</UserControl>
